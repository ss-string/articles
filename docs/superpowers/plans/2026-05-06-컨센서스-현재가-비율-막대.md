# 컨센서스 현재가 비율 막대 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 컨센서스 랭킹 테이블의 막대를 현재가 / 적정주가 비율 기준으로 표시하고, 100% 초과 상태를 배지로 표시한다.

**Architecture:** 변경은 `ConsensusTable` 내부의 표시 계산과 해당 CSS에 한정한다. 데이터 모델과 정렬 로직은 유지하고, 테스트는 DOM의 `aria-label`, 스타일 너비, 초과 배지 텍스트를 검증한다.

**Tech Stack:** React 19, TypeScript, Vitest, Testing Library, CSS

---

### Task 1: 현재가 비율 막대 테스트와 구현

**Files:**
- Modify: `src/components/ConsensusTable.test.tsx`
- Modify: `src/components/ConsensusTable.tsx`
- Modify: `src/styles.css`

- [ ] **Step 1: Write the failing tests**

Add these tests to `src/components/ConsensusTable.test.tsx` inside `describe('ConsensusTable', () => { ... })`:

```tsx
it('fills the price position bar by current price relative to fair price', () => {
  render(
    <ConsensusTable
      rows={[makeRow({ currentPrice: 55000, fairPrice: 100000, gapAmount: 45000, gapPercent: 81.8 })]}
      onSelect={() => undefined}
    />,
  );

  const bar = screen.getByLabelText('현재가가 적정주가의 55.0% 수준입니다.');
  const fill = bar.querySelector('span');

  expect(fill).toHaveStyle({ width: '55%' });
  expect(screen.getByText('55.0%', { selector: '.price-position-value' })).toBeInTheDocument();
  expect(screen.queryByText(/초과/)).not.toBeInTheDocument();
});

it('caps the price position bar and marks prices above fair price as overflow', () => {
  render(
    <ConsensusTable
      rows={[makeRow({ currentPrice: 130000, fairPrice: 100000, gapAmount: -30000, gapPercent: -23.1 })]}
      onSelect={() => undefined}
    />,
  );

  const bar = screen.getByLabelText('현재가가 적정주가의 130.0% 수준으로 초과했습니다.');
  const fill = bar.querySelector('span');

  expect(bar).toHaveClass('is-overflow');
  expect(fill).toHaveStyle({ width: '100%' });
  expect(screen.getByText('초과 130.0%', { selector: '.price-position-overflow' })).toBeInTheDocument();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk npm test -- src/components/ConsensusTable.test.tsx`

Expected: FAIL because the accessible labels and price position classes do not exist yet.

- [ ] **Step 3: Write minimal implementation**

In `src/components/ConsensusTable.tsx`, add helper functions near the existing helpers:

```tsx
function getPricePositionPercent(row: ConsensusRankingRow) {
  return (row.currentPrice / row.fairPrice) * 100;
}

function formatPricePositionPercent(value: number) {
  return `${value.toFixed(1)}%`;
}
```

Inside the row render, compute:

```tsx
const pricePositionPercent = getPricePositionPercent(row);
const cappedPricePositionPercent = Math.min(Math.max(pricePositionPercent, 0), 100);
const isPricePositionOverflow = pricePositionPercent > 100;
```

Replace the existing `.gap-bar` block in the last cell with:

```tsx
<div className="price-position">
  <div
    className={`price-position-bar${isPricePositionOverflow ? ' is-overflow' : ''}`}
    aria-label={`현재가가 적정주가의 ${formatPricePositionPercent(pricePositionPercent)} 수준${
      isPricePositionOverflow ? '으로 초과했습니다.' : '입니다.'
    }`}
  >
    <span style={{ width: `${cappedPricePositionPercent}%` }} />
  </div>
  {isPricePositionOverflow ? (
    <span className="price-position-overflow">초과 {formatPricePositionPercent(pricePositionPercent)}</span>
  ) : (
    <span className="price-position-value">{formatPricePositionPercent(pricePositionPercent)}</span>
  )}
</div>
```

In `src/styles.css`, add styles near `.gap-bar`:

```css
.price-position {
  display: grid;
  grid-template-columns: minmax(74px, 1fr) auto;
  align-items: center;
  gap: 8px;
  margin-bottom: 8px;
}

.price-position-bar {
  width: 100%;
  height: 7px;
  overflow: hidden;
  background: #e7edf3;
  border-radius: 999px;
}

.price-position-bar span {
  display: block;
  width: 0;
  height: 100%;
  max-width: 100%;
  background: #0f766e;
  border-radius: inherit;
}

.price-position-bar.is-overflow span {
  background: #dc2626;
}

.price-position-value,
.price-position-overflow {
  color: #0f766e;
  font-size: 12px;
  font-weight: 850;
  line-height: 1.2;
  white-space: nowrap;
}

.price-position-overflow {
  display: inline-flex;
  align-items: center;
  min-height: 24px;
  padding: 4px 8px;
  border: 1px solid #fecaca;
  border-radius: 8px;
  background: #fef2f2;
  color: #dc2626;
  font-weight: 900;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk npm test -- src/components/ConsensusTable.test.tsx`

Expected: PASS.

- [ ] **Step 5: Run full verification**

Run: `rtk npm test`

Expected: 18 test files pass.
