# 상세 progress 그래프 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 상세 팝업의 가격 비교 progress를 `현재가 / 적정가` 기준으로 바꾸고, B안인 3지표 + progress 설명 구조로 표시한다.

**Architecture:** `ConsensusDetailModal` 내부에 현재가 비율, capped width, 초과 여부, 갭 설명을 계산하는 작은 표시 helper를 둔다. 기존 공용 모델과 상세 추세 차트는 건드리지 않고, CSS는 상세 가격 비교 카드 전용 클래스만 추가한다.

**Tech Stack:** React 19, TypeScript, Vitest, Testing Library, CSS

---

### Task 1: 상세 가격 비교 progress

**Files:**
- Modify: `src/components/ConsensusDetailModal.test.tsx`
- Modify: `src/components/ConsensusDetailModal.tsx`
- Modify: `src/styles.css`

- [x] **Step 1: Write failing tests**

Add these tests to `src/components/ConsensusDetailModal.test.tsx` inside `describe('ConsensusDetailModal', () => { ... })`:

```tsx
it('renders price comparison progress by current price relative to fair price', () => {
  render(
    <ConsensusDetailModal
      row={makeRow({ currentPrice: 55000, fairPrice: 100000, gapAmount: 45000, gapPercent: 81.8 })}
      onClose={() => undefined}
    />,
  );

  const bar = screen.getByLabelText('현재가가 적정가의 55.0% 수준입니다.');
  const fill = bar.querySelector('span');

  expect(screen.getByText('적정가', { selector: '.detail-price-metric span' })).toBeInTheDocument();
  expect(screen.getByText('갭', { selector: '.detail-price-metric span' })).toBeInTheDocument();
  expect(screen.getByText('+45,000원', { selector: '.detail-gap-amount' })).toBeInTheDocument();
  expect(screen.getByText('+81.8%', { selector: '.detail-gap-percent' })).toBeInTheDocument();
  expect(fill).toHaveStyle({ width: '55%' });
  expect(screen.getByText('55.0%', { selector: '.detail-progress-value' })).toBeInTheDocument();
  expect(screen.getByText('적정가까지 45,000원 남았습니다.')).toBeInTheDocument();
});

it('caps detail price progress and marks prices above fair price as overflow', () => {
  render(
    <ConsensusDetailModal
      row={makeRow({ currentPrice: 130000, fairPrice: 100000, gapAmount: -30000, gapPercent: -23.1 })}
      onClose={() => undefined}
    />,
  );

  const bar = screen.getByLabelText('현재가가 적정가의 130.0% 수준으로 초과했습니다.');
  const fill = bar.querySelector('span');

  expect(bar).toHaveClass('is-overflow');
  expect(fill).toHaveStyle({ width: '100%' });
  expect(screen.getByText('초과 130.0%', { selector: '.detail-progress-overflow' })).toBeInTheDocument();
  expect(screen.getByText('적정가를 30,000원 초과했습니다.')).toBeInTheDocument();
});
```

Update the existing broad render test so it expects `적정가` instead of `적정주가` in the price comparison card if needed.

- [x] **Step 2: Run focused test to verify RED**

Run:

```bash
rtk npm test -- src/components/ConsensusDetailModal.test.tsx
```

Expected: FAIL because the new detail progress labels/classes do not exist and the old `.gap-bar.large` still uses `gapPercent`.

- [x] **Step 3: Implement minimal component changes**

In `src/components/ConsensusDetailModal.tsx`:

1. Add helpers near `formatGapDescription`:

```tsx
function getPricePositionPercent(row: ConsensusRankingRow) {
  return (row.currentPrice / row.fairPrice) * 100;
}

function formatPricePositionPercent(value: number) {
  return `${value.toFixed(1)}%`;
}

function formatSignedWon(value: number) {
  return `${value >= 0 ? '+' : '-'}${formatWon(Math.abs(value))}`;
}
```

2. Change `formatGapDescription` to use `적정가`.
3. Inside `ConsensusDetailModal`, compute:

```tsx
const pricePositionPercent = getPricePositionPercent(row);
const cappedPricePositionPercent = Number(Math.min(Math.max(pricePositionPercent, 0), 100).toFixed(1));
const isPricePositionOverflow = pricePositionPercent > 100;
```

4. Replace the old `price-compare` + `.gap-bar.large` + muted description in the `가격 비교` card with:

```tsx
<div className="detail-price-metrics">
  <div className="detail-price-metric">
    <span>현재가</span>
    <strong>{formatWon(row.currentPrice)}</strong>
  </div>
  <div className="detail-price-metric">
    <span>적정가</span>
    <strong>{formatWon(row.fairPrice)}</strong>
  </div>
  <div className={`detail-price-metric detail-gap-metric ${row.gapPercent < 0 ? 'negative' : ''}`}>
    <span>갭</span>
    <strong className="detail-gap-amount">{formatSignedWon(row.gapAmount)}</strong>
    <em className="detail-gap-percent">{formatPercent(row.gapPercent)}</em>
  </div>
</div>
<div className="detail-price-progress">
  <div className="detail-progress-head">
    <strong>적정가 대비 현재가</strong>
    {isPricePositionOverflow ? (
      <span className="detail-progress-overflow">초과 {formatPricePositionPercent(pricePositionPercent)}</span>
    ) : (
      <span className="detail-progress-value">{formatPricePositionPercent(pricePositionPercent)}</span>
    )}
  </div>
  <div
    className={`detail-progress-bar${isPricePositionOverflow ? ' is-overflow' : ''}`}
    aria-label={`현재가가 적정가의 ${formatPricePositionPercent(pricePositionPercent)} 수준${
      isPricePositionOverflow ? '으로 초과했습니다.' : '입니다.'
    }`}
  >
    <span style={{ width: `${cappedPricePositionPercent}%` }} />
  </div>
  <p>{formatGapDescription(row.gapAmount)}</p>
</div>
```

- [x] **Step 4: Add scoped CSS**

In `src/styles.css`, near the existing detail price styles, add:

```css
.detail-price-metrics {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
}

.detail-price-metric {
  min-width: 0;
  padding: 12px;
  border: 1px solid #e3e7ee;
  border-radius: 8px;
  background: #f8fafc;
}

.detail-price-metric span,
.detail-price-progress p {
  color: #64748b;
  font-size: 11px;
  font-weight: 750;
}

.detail-price-metric strong,
.detail-price-metric em {
  display: block;
  overflow-wrap: anywhere;
  color: #152238;
  font-size: 16px;
  font-style: normal;
  line-height: 1.25;
}

.detail-gap-metric strong,
.detail-gap-metric em {
  color: #0f766e;
  font-weight: 900;
}

.detail-gap-metric.negative strong,
.detail-gap-metric.negative em {
  color: #dc2626;
}

.detail-price-progress {
  display: grid;
  gap: 8px;
  margin-top: 12px;
  padding: 12px;
  border: 1px solid #e3e7ee;
  border-radius: 8px;
  background: #ffffff;
}

.detail-progress-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
}

.detail-progress-bar {
  height: 10px;
  overflow: hidden;
  border-radius: 999px;
  background: #e7edf3;
}

.detail-progress-bar span {
  display: block;
  height: 100%;
  max-width: 100%;
  border-radius: inherit;
  background: #0f766e;
}

.detail-progress-bar.is-overflow span {
  background: #dc2626;
}

.detail-progress-value,
.detail-progress-overflow {
  color: #0f766e;
  font-size: 12px;
  font-weight: 850;
  white-space: nowrap;
}

.detail-progress-overflow {
  display: inline-flex;
  min-height: 24px;
  align-items: center;
  padding: 4px 8px;
  border: 1px solid #fecaca;
  border-radius: 8px;
  background: #fef2f2;
  color: #dc2626;
  font-weight: 900;
}
```

In the mobile media query, add `.detail-price-metrics` to the single-column grid rule with `.price-compare`.

- [x] **Step 5: Run focused test to verify GREEN**

Run:

```bash
rtk npm test -- src/components/ConsensusDetailModal.test.tsx
```

Expected: PASS.

- [x] **Step 6: Run full verification**

Run:

```bash
rtk npm test
rtk npm run build
```

Expected: all tests pass and build succeeds.
