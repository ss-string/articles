# 인공지능 투자 리포트 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `금융 > AI 분석 리포트`에서 `investment_recommendation_reports`를 읽고, 상단 검색으로 선택한 종목의 리포트 이력, 종합 지면, 에이전트별 상세 의견을 실제 화면으로 제공한다.

**Architecture:** 데이터 정규화는 `src/ai-reports/model.ts`에 모으고, Supabase 조회는 `src/ai-reports/api.ts`, React 로딩 상태는 `src/ai-reports/useAiInvestmentReports.ts`에 둔다. 화면은 `src/components/AiAnalysisReportsPage.tsx`가 정규화된 모델만 받아 렌더링하며, 기존 분석자료실 CSS 구조를 유지하면서 `src/styles.css`에 AI 리포트 전용 클래스만 추가한다.

**Tech Stack:** React 19, TypeScript, Vitest, Testing Library, Supabase JS, Vite

---

## 파일 구조

- 생성: `src/ai-reports/model.ts`
  - raw DB row를 화면 모델로 정규화한다.
  - 최신 대표 종목 dedupe, 종목별 이력 정렬, Markdown 토큰화, 가격/날짜/점수 포맷을 담당한다.
- 생성: `src/ai-reports/model.test.ts`
  - 삼성전자 형태의 raw row로 모델 정규화와 최신 row 선택 정책을 검증한다.
- 생성: `src/ai-reports/api.ts`
  - `investment_recommendation_reports` 조회와 Supabase 환경변수 오류 처리를 담당한다.
- 생성: `src/ai-reports/useAiInvestmentReports.ts`
  - API 호출, 로딩/성공/오류 상태, 모델 빌드를 담당한다.
- 생성: `src/ai-reports/useAiInvestmentReports.test.tsx`
  - 훅 상태 전환과 Supabase query shape를 검증한다.
- 수정: `src/components/AiAnalysisReportsPage.tsx`
  - 상단 종목 검색, 선택 종목 이력, 점수 선택, 종합 지면, 에이전트 지면을 렌더링한다.
- 수정: `src/components/AiAnalysisReportsPage.test.tsx`
  - 컴포넌트 요구사항 전체를 사용자 동작 기준으로 검증한다.
- 수정: `src/App.tsx`
  - 테스트 주입용 `queryAiInvestmentReportRows` prop을 추가하고 AI 리포트 페이지에 전달한다.
- 수정: `src/App.test.tsx`
  - 금융 탭에서 AI 분석 리포트로 이동했을 때 주입 데이터를 표시하는지 검증한다.
- 수정: `src/styles.css`
  - 기존 카드/섹션 스타일을 이어받는 AI 리포트 레이아웃과 뱃지 스타일을 추가한다.

## Task 1: 데이터 모델, API, 훅

**Files:**
- Create: `src/ai-reports/model.ts`
- Create: `src/ai-reports/model.test.ts`
- Create: `src/ai-reports/api.ts`
- Create: `src/ai-reports/useAiInvestmentReports.ts`
- Create: `src/ai-reports/useAiInvestmentReports.test.tsx`

- [ ] **Step 1: 모델 실패 테스트 작성**

`src/ai-reports/model.test.ts`에 다음 행동을 검증하는 테스트를 작성한다.

```ts
import {
  buildAiInvestmentReportCatalog,
  formatKoreanWon,
  parseMarkdownBlocks,
  selectReportHistory,
} from './model';

function createSamsungRow(overrides: Record<string, unknown> = {}) {
  return {
    id: 'latest',
    market: 'KR',
    stock_code: '005930',
    stock_name: '삼성전자',
    issue_date: '2026-05-17',
    recommendation: '매수',
    current_price: 273500,
    total_score: 69,
    momentum_score: 78,
    technical_score: 74,
    valuation_score: 56,
    content_md: '## 투자 의견\n\n**매수** 관점입니다.\n\n### 핵심\n\nAI 수요가 확인됩니다.',
    report_payload: {
      actionPlan: {
        hold: '보유자는 유지',
        entry: '분할 진입',
        exitOrReview: '리스크 재점검',
      },
      bullFindings: ['목표주가 상향'],
      bearFindings: ['파운드리 적자'],
      riskChecklist: ['환율 변동'],
      investmentThesis: 'AI 반도체 수요와 목표주가 상향이 핵심입니다.',
    },
    agent_outputs: {
      momentum: {
        summary: '모멘텀은 우호적입니다.',
        score: 78,
        stance: 'bull',
        findings: [
          { type: 'bull', evidence: '목표주가 상승', confidence: 'high', metric: 'target_price' },
          { type: 'bull', evidence: '상승여력 23.77%', confidence: 'high', metric: 'upside' },
          { type: 'bull', evidence: 'AI 제품 수요', confidence: 'medium', metric: 'ai_demand' },
          { type: 'bull', evidence: '골디락스 레짐', confidence: 'medium', metric: 'macro' },
          { type: 'bear', evidence: '파운드리 단기 적자', confidence: 'medium', metric: 'foundry' },
          { type: 'bear', evidence: '3개월 추세 down', confidence: 'low', metric: 'trend' },
        ],
        risks: ['원문 검증 제한', '파운드리 적자', '거시경제 불확실성', 'CPI 상승', 'DX 수요 부진'],
        dataQuality: {
          limitations: ['입력 데이터 제한', '가격 시계열 없음', '원문 검증 제한', '동종업계 비교 불가', '시장 전체 배경'],
        },
      },
    },
    created_at: '2026-05-17T08:00:00+00:00',
    updated_at: '2026-05-17T09:26:10.444135+00:00',
    ...overrides,
  };
}
```

검증 항목:
- `buildAiInvestmentReportCatalog([older, latest])`는 같은 `stock_code` 대표 종목에 latest만 남긴다.
- 대표 종목과 이력은 `updated_at` 내림차순이다.
- `current_price`는 숫자와 표시 문자열로 보존한다.
- `report_payload.actionPlan`, `bullFindings`, `bearFindings`, `riskChecklist`를 보존한다.
- `agent_outputs.momentum.findings` 6개, `risks` 5개, `limitations` 5개가 모두 보존된다.
- finding의 `metric`은 모델에 남겨도 되지만 UI 노출용 필드에는 포함하지 않는다.
- `formatKoreanWon(273500)`은 `273,500원`, 값이 없으면 `현재가 없음`이다.
- `parseMarkdownBlocks`는 `##`, `###`, 문단, `**굵게**`를 구분한다.

- [ ] **Step 2: 모델 테스트 실패 확인**

Run: `npm test -- src/ai-reports/model.test.ts`

Expected: 파일 또는 export가 없어서 실패한다.

- [ ] **Step 3: 모델 최소 구현**

`src/ai-reports/model.ts`에 다음 public API를 구현한다.

```ts
export type RawAiInvestmentReportRow = Record<string, unknown>;
export type AiReportScoreKey = 'total' | 'momentum' | 'technical' | 'valuation';
export type AgentKey = 'momentum' | 'technical' | 'valuation';
export type MarkdownBlock =
  | { type: 'heading'; level: 2 | 3; text: string }
  | { type: 'paragraph'; parts: Array<{ text: string; strong: boolean }> };
export type AiAgentFinding = {
  type: string;
  evidence: string;
  confidence: string | null;
  metric: string | null;
};
export type AiAgentOutput = {
  key: AgentKey;
  label: string;
  summary: string | null;
  score: number | null;
  stance: string | null;
  findings: AiAgentFinding[];
  risks: string[];
  limitations: string[];
};
export type AiInvestmentReport = {
  id: string;
  market: string | null;
  stockCode: string;
  stockName: string;
  issueDate: string | null;
  recommendation: string | null;
  currentPrice: number | null;
  currentPriceLabel: string;
  totalScore: number | null;
  momentumScore: number | null;
  technicalScore: number | null;
  valuationScore: number | null;
  contentMd: string;
  markdownBlocks: MarkdownBlock[];
  actionPlan: { hold: string | null; entry: string | null; exitOrReview: string | null };
  bullFindings: string[];
  bearFindings: string[];
  riskChecklist: string[];
  investmentThesis: string | null;
  agents: Record<AgentKey, AiAgentOutput | null>;
  createdAt: string | null;
  updatedAt: string | null;
  displayUpdatedAt: string | null;
  sortTimestamp: number;
};
export type AiInvestmentReportCatalog = {
  representativeReports: AiInvestmentReport[];
  reports: AiInvestmentReport[];
};
export function normalizeAiInvestmentReport(row: RawAiInvestmentReportRow): AiInvestmentReport | null;
export function buildAiInvestmentReportCatalog(rows: RawAiInvestmentReportRow[]): AiInvestmentReportCatalog;
export function selectReportHistory(reports: AiInvestmentReport[], stockCode: string): AiInvestmentReport[];
export function formatKoreanWon(value: unknown): string;
export function parseMarkdownBlocks(markdown: string): MarkdownBlock[];
```

구현 규칙:
- 필수값은 `stock_code`, `stock_name`이며 둘 중 하나라도 없으면 row를 버린다.
- 최신성은 `updated_at`, 없으면 `created_at`, 없으면 0으로 계산한다.
- 대표 종목은 `stockCode`별 최신 row만 남긴 뒤 최신순 정렬한다.
- 이력은 같은 `stockCode` 전체를 최신순 정렬한다.
- `contentMd`는 `row.content_md`를 우선 사용하고 없으면 `report_payload.contentMd`, 없으면 `본문이 없습니다.`를 사용한다.
- `actionPlan`은 `report_payload.actionPlan.hold`, `entry`, `exitOrReview`만 읽는다.
- agent label은 `Momentum Agent`, `Technical Agent`, `Valuation Agent`다.

- [ ] **Step 4: 모델 테스트 통과 확인**

Run: `npm test -- src/ai-reports/model.test.ts`

Expected: 모든 테스트가 통과한다.

- [ ] **Step 5: API/훅 실패 테스트 작성**

`src/ai-reports/useAiInvestmentReports.test.tsx`에 다음을 검증한다.
- Supabase 환경변수가 없으면 `Supabase 환경변수가 설정되지 않았습니다.` 오류가 반환된다.
- 주입한 `queryRows`가 raw row를 반환하면 훅이 catalog를 성공 상태로 반환한다.
- query 실패 시 오류 메시지를 반환한다.
- `queryAiInvestmentReportRows()`는 `investment_recommendation_reports`에서 필요한 컬럼을 select하고 `updated_at` 내림차순으로 조회한다.

- [ ] **Step 6: API/훅 테스트 실패 확인**

Run: `npm test -- src/ai-reports/useAiInvestmentReports.test.tsx`

Expected: 파일 또는 export가 없어서 실패한다.

- [ ] **Step 7: API/훅 최소 구현**

`src/ai-reports/api.ts`:
- `getAiInvestmentReportsSupabaseConfig()`
- `queryAiInvestmentReportRows(): Promise<RawAiInvestmentReportRow[]>`
- select 컬럼: `id, market, stock_code, stock_name, issue_date, recommendation, current_price, total_score, valuation_score, momentum_score, technical_score, content_md, report_payload, agent_outputs, source_payload, created_at, updated_at`
- `.order('updated_at', { ascending: false })`

`src/ai-reports/useAiInvestmentReports.ts`:
- 상태 union: loading, success, error
- 기본 query는 `queryAiInvestmentReportRows`
- 성공 시 `buildAiInvestmentReportCatalog(rows)`를 반환
- 오류 fallback 문구는 `AI 분석 리포트를 불러오지 못했습니다.`

- [ ] **Step 8: API/훅 테스트 통과 확인**

Run: `npm test -- src/ai-reports/model.test.ts src/ai-reports/useAiInvestmentReports.test.tsx`

Expected: 모든 테스트가 통과한다.

## Task 2: AI 리포트 지면 UI와 App 연결

**Files:**
- Modify: `src/components/AiAnalysisReportsPage.tsx`
- Modify: `src/components/AiAnalysisReportsPage.test.tsx`
- Modify: `src/App.tsx`
- Modify: `src/App.test.tsx`
- Modify: `src/styles.css`

- [ ] **Step 1: 컴포넌트 실패 테스트 작성**

`src/components/AiAnalysisReportsPage.test.tsx`에 다음 사용자 동작 테스트를 작성한다.
- 로딩 상태 문구: `AI 분석 리포트를 불러오는 중입니다.`
- 빈 상태 문구: `표시할 AI 분석 리포트가 없습니다.`
- 상단 검색은 `stock_name`과 `stock_code`로 대표 종목을 찾는다.
- 검색 결과의 대표 종목은 동일 `stock_code` 중 최신 row만 사용한다.
- 좌측 목록은 전체 대표 종목이 아니라 선택된 종목의 리포트 이력만 노출한다.
- 선택된 종목의 이력은 `updated_at` 기준 최신순에서 과거순으로 정렬한다.
- 상세 상단에는 선택 리포트 분석 결과와 투자 가설 요약을 표시한다.
- 삼성전자 대표 카드를 선택하면 같은 종목 이력이 최신순으로 모두 노출된다.
- 기본 선택은 `totalScore`이며 종합 지면에서 Markdown `## 투자 의견`, `### 핵심`이 제목으로 렌더링된다.
- 추천 박스에는 `매수`와 `totalScore 69`만 보이고 `DB recommendation`은 없다.
- actionPlan 박스 상단에는 `현재가`와 `273,500원`이 보인다.
- 종합 지면의 `bullFindings`, `bearFindings`에는 `bull`/`bear` 뱃지가 없다.
- `momentum` 점수 카드를 누르면 agent 지면으로 바뀌고 summary, findings, risks, limitations가 보인다.
- momentum findings 6개가 모두 보이며, confidence는 `high`처럼 값만 보인다.
- agent finding에는 metric 텍스트가 보이지 않는다.
- risks 5개와 limitations 5개가 모두 보인다.
- 이전 리포트를 누르면 이전 row의 본문, 점수, 현재가가 반영된다.

- [ ] **Step 2: App 연결 실패 테스트 작성**

`src/App.test.tsx`의 금융 탭 이동 테스트에 `queryAiInvestmentReportRows`를 주입하고, AI 분석 리포트 탭 진입 후 삼성전자 데이터가 표시되는지 검증한다.

- [ ] **Step 3: UI 테스트 실패 확인**

Run: `npm test -- src/components/AiAnalysisReportsPage.test.tsx src/App.test.tsx`

Expected: 기존 빈 상태 구현 때문에 데이터 표시 기대가 실패한다.

- [ ] **Step 4: 컴포넌트 구현**

`AiAnalysisReportsPage` props:

```ts
type AiAnalysisReportsPageProps = {
  queryRows?: () => Promise<RawAiInvestmentReportRow[]>;
};
```

구현 규칙:
- `useAiInvestmentReports({ queryRows })`를 호출한다.
- 첫 대표 리포트를 기본 선택한다.
- 선택된 대표 리포트의 `stockCode`로 `selectReportHistory(catalog.reports, stockCode)`를 만들고 첫 이력을 기본 선택한다.
- 선택 이력이 바뀌면 선택 점수는 `total`로 되돌린다.
- 점수 버튼은 `totalScore`, `momentum`, `technical`, `valuation` 네 개다.
- `total` 선택 시 종합 지면만 렌더링하고 agent 상세 패널을 만들지 않는다.
- agent 선택 시 `summary`, `findings`, `risks`, `limitations` 네 영역을 렌더링한다.
- finding 카드는 `type`, `confidence`, `evidence`만 표시하고 `metric`은 렌더링하지 않는다.
- 이력 버튼을 누르면 이전 row 기준으로 모든 지면 데이터가 갱신된다.

- [ ] **Step 5: App 연결 구현**

`src/App.tsx`:
- `RawAiInvestmentReportRow` 타입을 import한다.
- `AppProps`에 `queryAiInvestmentReportRows?: () => Promise<RawAiInvestmentReportRow[]>`를 추가한다.
- AI 리포트 route에서 `<AiAnalysisReportsPage queryRows={queryAiInvestmentReportRows} />`로 전달한다.

- [ ] **Step 6: CSS 구현**

`src/styles.css`에 `ai-report-section` 주변 전용 스타일을 추가한다.
- 기존 `analysis-section`, `state-panel`, 카드 스타일과 같은 8px radius를 유지한다.
- 상단 검색, 좌측 선택 종목 이력, 우측 지면은 desktop에서 이력/상세 2열, 좁은 화면에서 1열이다.
- 점수 카드는 버튼이며 active 상태가 명확해야 한다.
- `bull`, `bear`, `confidence`는 작은 badge로 표현한다.
- agent finding, risk, limitation 목록은 내용이 길어도 카드 밖으로 넘치지 않게 `overflow-wrap: anywhere`를 적용한다.

- [ ] **Step 7: UI 테스트 통과 확인**

Run: `npm test -- src/components/AiAnalysisReportsPage.test.tsx src/App.test.tsx`

Expected: 모든 테스트가 통과한다.

## Task 3: 통합 검증과 브라우저 확인

**Files:**
- Verify only

- [ ] **Step 1: 전체 테스트 실행**

Run: `npm test`

Expected: 전체 테스트 통과.

- [ ] **Step 2: 프로덕션 빌드 실행**

Run: `npm run build`

Expected: TypeScript와 Vite 빌드 통과.

- [ ] **Step 3: 개발 서버 실행**

Run: `npm run dev -- --port 52218`

Expected: `http://localhost:52218/articles/finance/ai-reports`에서 확인 가능.

## 자체 검토

- 설계 요구사항 매핑:
  - 최신 대표 dedupe: Task 1 모델 테스트와 구현.
  - 상단 검색과 선택 종목 이력 최신순: Task 2 UI 테스트.
  - totalScore 기본 지면 유지: Task 2 컴포넌트 테스트.
  - agent 선택 시 summary/findings/risks/limitations: Task 2 컴포넌트 테스트.
  - findings 전체 렌더링, evidence/confidence 노출, metric 숨김: Task 1/2 테스트.
  - content_md 원문 Markdown 렌더링: Task 1 parser, Task 2 UI 테스트.
  - actionPlan 상단 현재가: Task 1 format, Task 2 UI 테스트.
- placeholder 검토: 모든 단계는 파일, 명령, 기대 결과를 포함한다.
- 타입 일관성 검토: `RawAiInvestmentReportRow`, `AiInvestmentReport`, `AiReportScoreKey`, `AgentKey`를 Task 1에서 정의하고 Task 2에서 그대로 사용한다.
