# 화이트톤 분석자료실 라우팅 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 단일 포트폴리오 대시보드를 화이트톤 분석자료실 앱으로 바꾸고, `메인 > 매크로 레짐`과 `금융 > 핫 뉴스 분석 리포트 / 컨센서스 괴리율 랭킹 / AI 분석 리포트` 라우팅을 제공한다.

**Architecture:** 새 의존성을 추가하지 않고 `window.location`과 `history.pushState` 기반의 작은 라우팅 헬퍼를 만든다. `App`은 현재 경로를 상태로 들고, 공통 앱 셸은 `AppShell` 컴포넌트가 담당한다. 기존 데이터 페이지 컴포넌트는 재사용하고, 신규 `AI 분석 리포트`는 초기 빈 상태 페이지로 추가한다.

**Tech Stack:** React 19, TypeScript, Vite, Vitest, Testing Library, CSS.

---

## 파일 구조

- Create: `src/navigation.ts` - 앱 라우트, 메뉴 심볼, 경로 판별, 경로 이동 이벤트 헬퍼.
- Create: `src/navigation.test.ts` - 라우트 판별과 상위 메뉴별 하위 메뉴 노출 규칙 테스트.
- Create: `src/components/AppShell.tsx` - 최상단 행, 데스크탑/모바일 사이드바, 금융 언더라인 탭, 본문 슬롯.
- Create: `src/components/AppShell.test.tsx` - 사이드바 토글, 모바일 딤드 닫기, 상위 메뉴별 하위 메뉴 노출, 탭 이동 테스트.
- Create: `src/components/AiAnalysisReportsPage.tsx` - AI 분석 리포트 초기 화면과 빈 상태.
- Create: `src/components/AiAnalysisReportsPage.test.tsx` - 신규 페이지 렌더링 테스트.
- Modify: `src/App.tsx` - 현재 경로에 따라 페이지를 하나만 렌더링하도록 변경.
- Modify: `src/App.test.tsx` - 기존 해시 섹션 테스트를 라우팅 테스트로 교체하고 상세 팝업 회귀를 유지.
- Modify: `src/styles.css` - 어두운 포트폴리오 스타일을 화이트톤 분석자료실 스타일로 전환.
- Modify: `src/content.ts` - 기존 포트폴리오 내비게이션 배열을 사용하지 않게 정리.

---

### Task 1: 라우팅 모델과 메뉴 규칙

**Files:**
- Create: `src/navigation.ts`
- Create: `src/navigation.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `src/navigation.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import {
  financeRoutes,
  getActiveRoute,
  getVisibleNavigation,
  mainRoutes,
  normalizePathname,
  routes,
} from './navigation';

describe('navigation', () => {
  it('normalizes supported and unknown paths to the active route', () => {
    expect(normalizePathname('/')).toBe('/main/macro-regime');
    expect(normalizePathname('/main/macro-regime')).toBe('/main/macro-regime');
    expect(normalizePathname('/finance/hot-news')).toBe('/finance/hot-news');
    expect(normalizePathname('/finance/consensus')).toBe('/finance/consensus');
    expect(normalizePathname('/finance/ai-reports')).toBe('/finance/ai-reports');
    expect(normalizePathname('/unknown')).toBe('/main/macro-regime');
  });

  it('returns route metadata with symbols and section labels', () => {
    expect(getActiveRoute('/finance/consensus')).toEqual({
      path: '/finance/consensus',
      section: 'finance',
      label: '컨센서스 괴리율 랭킹',
      shortLabel: '컨센서스 랭킹',
      kicker: '금융 밸류에이션',
      symbol: '↗',
    });
    expect(routes.map((route) => route.symbol)).toEqual(['◷', '≡', '↗', '⌁']);
    expect(mainRoutes.map((route) => route.label)).toEqual(['매크로 레짐']);
    expect(financeRoutes.map((route) => route.label)).toEqual([
      '핫 뉴스 분석 리포트',
      '컨센서스 괴리율 랭킹',
      'AI 분석 리포트',
    ]);
  });

  it('shows only the active top-level section children', () => {
    expect(getVisibleNavigation('/main/macro-regime')).toEqual([
      {
        key: 'main',
        label: '메인',
        symbol: '⌂',
        active: true,
        children: [routes[0]],
      },
      {
        key: 'finance',
        label: '금융',
        symbol: '◆',
        active: false,
        children: [],
      },
    ]);

    expect(getVisibleNavigation('/finance/hot-news')).toEqual([
      {
        key: 'main',
        label: '메인',
        symbol: '⌂',
        active: false,
        children: [],
      },
      {
        key: 'finance',
        label: '금융',
        symbol: '◆',
        active: true,
        children: financeRoutes,
      },
    ]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/navigation.test.ts --run
```

Expected: FAIL because `src/navigation.ts` does not exist.

- [ ] **Step 3: Implement the navigation model**

Create `src/navigation.ts`:

```ts
export type AppSection = 'main' | 'finance';

export type AppRoute = {
  path: string;
  section: AppSection;
  label: string;
  shortLabel: string;
  kicker: string;
  symbol: string;
};

export type TopLevelNavigationItem = {
  key: AppSection;
  label: string;
  symbol: string;
  active: boolean;
  children: AppRoute[];
};

export const mainRoutes: AppRoute[] = [
  {
    path: '/main/macro-regime',
    section: 'main',
    label: '매크로 레짐',
    shortLabel: '매크로 레짐',
    kicker: '시장 국면 분석',
    symbol: '◷',
  },
];

export const financeRoutes: AppRoute[] = [
  {
    path: '/finance/hot-news',
    section: 'finance',
    label: '핫 뉴스 분석 리포트',
    shortLabel: '핫 뉴스 분석',
    kicker: '금융 이슈',
    symbol: '≡',
  },
  {
    path: '/finance/consensus',
    section: 'finance',
    label: '컨센서스 괴리율 랭킹',
    shortLabel: '컨센서스 랭킹',
    kicker: '금융 밸류에이션',
    symbol: '↗',
  },
  {
    path: '/finance/ai-reports',
    section: 'finance',
    label: 'AI 분석 리포트',
    shortLabel: 'AI 분석',
    kicker: '자동 분석 요약',
    symbol: '⌁',
  },
];

export const routes: AppRoute[] = [...mainRoutes, ...financeRoutes];

export function normalizePathname(pathname: string) {
  if (pathname === '/') {
    return mainRoutes[0].path;
  }

  return routes.some((route) => route.path === pathname) ? pathname : mainRoutes[0].path;
}

export function getActiveRoute(pathname: string) {
  const normalizedPath = normalizePathname(pathname);
  return routes.find((route) => route.path === normalizedPath) ?? mainRoutes[0];
}

export function getVisibleNavigation(pathname: string): TopLevelNavigationItem[] {
  const activeRoute = getActiveRoute(pathname);

  return [
    {
      key: 'main',
      label: '메인',
      symbol: '⌂',
      active: activeRoute.section === 'main',
      children: activeRoute.section === 'main' ? mainRoutes : [],
    },
    {
      key: 'finance',
      label: '금융',
      symbol: '◆',
      active: activeRoute.section === 'finance',
      children: activeRoute.section === 'finance' ? financeRoutes : [],
    },
  ];
}

export function navigateToPath(path: string) {
  const normalizedPath = normalizePathname(path);
  window.history.pushState({}, '', normalizedPath);
  window.dispatchEvent(new PopStateEvent('popstate'));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```bash
npm test -- src/navigation.test.ts --run
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/navigation.ts src/navigation.test.ts
git commit -m "분석자료실 라우팅 모델 추가"
```

---

### Task 2: AI 분석 리포트 초기 페이지

**Files:**
- Create: `src/components/AiAnalysisReportsPage.tsx`
- Create: `src/components/AiAnalysisReportsPage.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `src/components/AiAnalysisReportsPage.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { AiAnalysisReportsPage } from './AiAnalysisReportsPage';

describe('AiAnalysisReportsPage', () => {
  it('renders a ready empty state for the new finance route', () => {
    render(<AiAnalysisReportsPage />);

    expect(screen.getByRole('heading', { name: 'AI 분석 리포트' })).toBeInTheDocument();
    expect(screen.getByText('자동 분석 요약')).toBeInTheDocument();
    expect(screen.getByText('표시할 AI 분석 리포트가 없습니다.')).toBeInTheDocument();
    expect(screen.getByText('AI 분석 리포트 데이터 모델과 조회 방식은 별도 구현에서 연결합니다.')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/components/AiAnalysisReportsPage.test.tsx --run
```

Expected: FAIL because `AiAnalysisReportsPage` does not exist.

- [ ] **Step 3: Implement the component**

Create `src/components/AiAnalysisReportsPage.tsx`:

```tsx
export function AiAnalysisReportsPage() {
  return (
    <section className="analysis-section ai-report-section" aria-labelledby="ai-report-title">
      <div className="section-heading">
        <span>자동 분석 요약</span>
        <h2 id="ai-report-title">AI 분석 리포트</h2>
      </div>
      <div className="state-panel">
        <strong>표시할 AI 분석 리포트가 없습니다.</strong>
        <p>AI 분석 리포트 데이터 모델과 조회 방식은 별도 구현에서 연결합니다.</p>
      </div>
    </section>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```bash
npm test -- src/components/AiAnalysisReportsPage.test.tsx --run
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/AiAnalysisReportsPage.tsx src/components/AiAnalysisReportsPage.test.tsx
git commit -m "AI 분석 리포트 초기 페이지 추가"
```

---

### Task 3: 공통 앱 셸과 인터랙션

**Files:**
- Create: `src/components/AppShell.tsx`
- Create: `src/components/AppShell.test.tsx`

- [ ] **Step 1: Write the failing tests**

Create `src/components/AppShell.test.tsx`:

```tsx
import { render, screen, within } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { vi } from 'vitest';
import type { AppRoute } from '../navigation';
import { AppShell } from './AppShell';

const consensusRoute: AppRoute = {
  path: '/finance/consensus',
  section: 'finance',
  label: '컨센서스 괴리율 랭킹',
  shortLabel: '컨센서스 랭킹',
  kicker: '금융 밸류에이션',
  symbol: '↗',
};

const macroRoute: AppRoute = {
  path: '/main/macro-regime',
  section: 'main',
  label: '매크로 레짐',
  shortLabel: '매크로 레짐',
  kicker: '시장 국면 분석',
  symbol: '◷',
};

describe('AppShell', () => {
  it('renders the library brand in the sidebar but not in the top row', () => {
    render(
      <AppShell activeRoute={consensusRoute} onNavigate={vi.fn()}>
        <p>본문</p>
      </AppShell>,
    );

    expect(screen.getByRole('complementary', { name: '분석자료실 메뉴' })).toHaveTextContent('분석자료실');
    expect(screen.getByRole('banner')).not.toHaveTextContent('분석자료실');
    expect(screen.getByRole('button', { name: '사이드바 열기 또는 닫기' })).toBeInTheDocument();
  });

  it('shows only finance children when a finance route is active', () => {
    render(
      <AppShell activeRoute={consensusRoute} onNavigate={vi.fn()}>
        <p>본문</p>
      </AppShell>,
    );

    const sidebar = screen.getByRole('complementary', { name: '분석자료실 메뉴' });
    expect(within(sidebar).getByRole('button', { name: /◆ 금융/ })).toHaveClass('active');
    expect(within(sidebar).queryByRole('button', { name: /◷ 매크로 레짐/ })).not.toBeInTheDocument();
    expect(within(sidebar).getByRole('button', { name: /≡ 핫 뉴스 분석 리포트/ })).toBeInTheDocument();
    expect(within(sidebar).getByRole('button', { name: /↗ 컨센서스 괴리율 랭킹/ })).toHaveClass('active');
    expect(within(sidebar).getByRole('button', { name: /⌁ AI 분석 리포트/ })).toBeInTheDocument();
  });

  it('shows only main children when the main route is active', () => {
    render(
      <AppShell activeRoute={macroRoute} onNavigate={vi.fn()}>
        <p>본문</p>
      </AppShell>,
    );

    const sidebar = screen.getByRole('complementary', { name: '분석자료실 메뉴' });
    expect(within(sidebar).getByRole('button', { name: /⌂ 메인/ })).toHaveClass('active');
    expect(within(sidebar).getByRole('button', { name: /◷ 매크로 레짐/ })).toBeInTheDocument();
    expect(within(sidebar).queryByRole('button', { name: /≡ 핫 뉴스 분석 리포트/ })).not.toBeInTheDocument();
  });

  it('toggles the desktop sidebar from the top row', async () => {
    const user = userEvent.setup();
    const { container } = render(
      <AppShell activeRoute={consensusRoute} onNavigate={vi.fn()}>
        <p>본문</p>
      </AppShell>,
    );

    expect(container.querySelector('.app-shell')).not.toHaveClass('sidebar-collapsed');
    await user.click(screen.getByRole('button', { name: '사이드바 열기 또는 닫기' }));
    expect(container.querySelector('.app-shell')).toHaveClass('sidebar-collapsed');
  });

  it('opens the mobile drawer and closes it from the dimmed area without an X button', async () => {
    const user = userEvent.setup();
    const { container } = render(
      <AppShell activeRoute={consensusRoute} onNavigate={vi.fn()}>
        <p>본문</p>
      </AppShell>,
    );

    await user.click(screen.getByRole('button', { name: '모바일 사이드바 열기' }));
    expect(container.querySelector('.mobile-sidebar')).toHaveClass('open');
    expect(screen.queryByRole('button', { name: /닫기/ })).not.toBeInTheDocument();

    await user.click(screen.getByTestId('mobile-sidebar-dim'));
    expect(container.querySelector('.mobile-sidebar')).not.toHaveClass('open');
  });

  it('uses underline tabs for finance routes and calls onNavigate', async () => {
    const user = userEvent.setup();
    const onNavigate = vi.fn();
    render(
      <AppShell activeRoute={consensusRoute} onNavigate={onNavigate}>
        <p>본문</p>
      </AppShell>,
    );

    expect(screen.getByRole('tab', { name: '컨센서스 괴리율 랭킹' })).toHaveAttribute('aria-selected', 'true');
    await user.click(screen.getByRole('tab', { name: 'AI 분석 리포트' }));

    expect(onNavigate).toHaveBeenCalledWith('/finance/ai-reports');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/components/AppShell.test.tsx --run
```

Expected: FAIL because `AppShell` does not exist.

- [ ] **Step 3: Implement `AppShell`**

Create `src/components/AppShell.tsx`:

```tsx
import { useState, type ReactNode } from 'react';
import { financeRoutes, getVisibleNavigation, type AppRoute } from '../navigation';

type AppShellProps = {
  activeRoute: AppRoute;
  onNavigate: (path: string) => void;
  children: ReactNode;
};

export function AppShell({ activeRoute, onNavigate, children }: AppShellProps) {
  const [isSidebarCollapsed, setIsSidebarCollapsed] = useState(false);
  const [isMobileSidebarOpen, setIsMobileSidebarOpen] = useState(false);
  const navigationItems = getVisibleNavigation(activeRoute.path);

  function navigate(path: string) {
    onNavigate(path);
    setIsMobileSidebarOpen(false);
  }

  const sidebarContent = (
    <>
      <a className="brand" href="/main/macro-regime" onClick={(event) => {
        event.preventDefault();
        navigate('/main/macro-regime');
      }}>
        <span className="brand-mark">A</span>
        <span>
          <strong>분석자료실</strong>
          <small>Research Library</small>
        </span>
      </a>
      <nav className="nav-list" aria-label="분석자료실 내비게이션">
        {navigationItems.map((item) => (
          <div className="nav-group" key={item.key}>
            <button
              className={`nav-item${item.active ? ' active' : ''}`}
              type="button"
              onClick={() => navigate(item.key === 'main' ? '/main/macro-regime' : '/finance/hot-news')}
            >
              <span className="menu-symbol" aria-hidden="true">{item.symbol}</span>
              <span>{item.label}</span>
            </button>
            {item.children.length > 0 ? (
              <div className="sub-list">
                {item.children.map((child) => (
                  <button
                    className={`sub-item${child.path === activeRoute.path ? ' active' : ''}`}
                    type="button"
                    key={child.path}
                    onClick={() => navigate(child.path)}
                  >
                    <span className="sub-symbol" aria-hidden="true">{child.symbol}</span>
                    <span>{child.label}</span>
                  </button>
                ))}
              </div>
            ) : null}
          </div>
        ))}
      </nav>
    </>
  );

  return (
    <div className={`app-shell${isSidebarCollapsed ? ' sidebar-collapsed' : ''}`}>
      <aside className="sidebar" aria-label="분석자료실 메뉴">
        {sidebarContent}
      </aside>
      <div className="workspace">
        <header className="top-nav">
          <div className="top-nav-left">
            <button
              className="sidebar-toggle desktop-toggle"
              type="button"
              aria-label="사이드바 열기 또는 닫기"
              onClick={() => setIsSidebarCollapsed((value) => !value)}
            >
              ☰
            </button>
            <button
              className="sidebar-toggle mobile-toggle"
              type="button"
              aria-label="모바일 사이드바 열기"
              onClick={() => setIsMobileSidebarOpen(true)}
            >
              ☰
            </button>
          </div>
          <div className="top-nav-actions" aria-label="예정된 전역 액션">
            <span>로그인</span>
            <span>설정</span>
          </div>
        </header>
        <main className="dashboard">
          <header className="page-header">
            <span>{activeRoute.kicker}</span>
            <h1>{activeRoute.label}</h1>
          </header>
          {activeRoute.section === 'finance' ? (
            <nav className="finance-tabs" aria-label="금융 하위 페이지" role="tablist">
              {financeRoutes.map((route) => (
                <button
                  type="button"
                  role="tab"
                  aria-selected={route.path === activeRoute.path}
                  className={`finance-tab${route.path === activeRoute.path ? ' active' : ''}`}
                  key={route.path}
                  onClick={() => navigate(route.path)}
                >
                  {route.label}
                </button>
              ))}
            </nav>
          ) : null}
          {children}
        </main>
      </div>
      <div
        className={`mobile-sidebar-dim${isMobileSidebarOpen ? ' visible' : ''}`}
        data-testid="mobile-sidebar-dim"
        onClick={() => setIsMobileSidebarOpen(false)}
      />
      <aside className={`mobile-sidebar${isMobileSidebarOpen ? ' open' : ''}`} aria-label="모바일 분석자료실 메뉴">
        {sidebarContent}
      </aside>
    </div>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```bash
npm test -- src/components/AppShell.test.tsx --run
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/AppShell.tsx src/components/AppShell.test.tsx
git commit -m "분석자료실 앱 셸 추가"
```

---

### Task 4: App 라우팅 통합

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.test.tsx`

- [ ] **Step 1: Replace `App.test.tsx` with route-first coverage**

Replace `src/App.test.tsx` with:

```tsx
import { render, screen, within } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import App from './App';
import type { RawHotNewsReportRow } from './hot-news/model';

const rows = [
  {
    stock_name: '삼성전자',
    stock_code: '005930',
    fnguide_code: 'A005930',
    current_price: 72400,
    target_price: 100200,
    consensus_1m: 93800,
    consensus_3m: 96300,
    consensus_6m: 91300,
  },
];

const macroRows = [
  {
    id: '2026-05-04-KR',
    run_date: '2026-05-04',
    market: 'KR',
    regime: '골디락스(goldilocks)',
    axis_assessments: {
      growth: { axis: 'expanding', rationale: '성장 확장', confidence: 0.82 },
      inflation: { axis: 'neutral', rationale: '물가 중립', confidence: 0.74 },
      monetary: { axis: 'neutral', rationale: '통화 중립', confidence: 0.63 },
      liquidity: { axis: 'ample', rationale: '유동성 우호', confidence: 0.68 },
    },
  },
];

const hotNewsRows: RawHotNewsReportRow[] = [
  {
    id: 1,
    issue_date: '2026-05-04',
    title: '2026-05-04 조선 에너지 수주',
    perspective: '조선 에너지 수주',
    tldr: ['국내 조선·해양 에너지 인프라 기업의 수주 뉴스가 집중'],
  },
];

describe('App routing', () => {
  afterEach(() => {
    window.history.replaceState({}, '', '/');
  });

  it('renders macro regime as the default main page', async () => {
    render(
      <App
        queryRows={async () => rows}
        queryHotNewsRows={async () => hotNewsRows}
        queryMacroRows={async () => macroRows}
        queryReports={async () => []}
      />,
    );

    expect(screen.getByRole('banner')).not.toHaveTextContent('분석자료실');
    expect(screen.getByRole('complementary', { name: '분석자료실 메뉴' })).toHaveTextContent('분석자료실');
    expect(screen.getByRole('button', { name: /⌂ 메인/ })).toHaveClass('active');
    expect(screen.getByRole('button', { name: /◷ 매크로 레짐/ })).toBeInTheDocument();
    expect(screen.queryByRole('button', { name: /≡ 핫 뉴스 분석 리포트/ })).not.toBeInTheDocument();
    expect(await screen.findByRole('heading', { name: '매크로 레짐' })).toBeInTheDocument();
    expect(await screen.findByRole('heading', { name: '최신 매크로 레짐 판단' })).toBeInTheDocument();
  });

  it('renders finance consensus route with finance children and underline tabs', async () => {
    window.history.pushState({}, '', '/finance/consensus');

    render(
      <App
        queryRows={async () => rows}
        queryHotNewsRows={async () => hotNewsRows}
        queryMacroRows={async () => macroRows}
        queryReports={async () => []}
      />,
    );

    expect(screen.getByRole('button', { name: /◆ 금융/ })).toHaveClass('active');
    expect(screen.getByRole('button', { name: /≡ 핫 뉴스 분석 리포트/ })).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /↗ 컨센서스 괴리율 랭킹/ })).toHaveClass('active');
    expect(screen.getByRole('button', { name: /⌁ AI 분석 리포트/ })).toBeInTheDocument();
    expect(screen.queryByRole('button', { name: /◷ 매크로 레짐/ })).not.toBeInTheDocument();
    expect(screen.getByRole('tab', { name: '컨센서스 괴리율 랭킹' })).toHaveAttribute('aria-selected', 'true');
    expect(await screen.findByText('삼성전자')).toBeInTheDocument();
  });

  it('navigates between finance pages from underline tabs', async () => {
    const user = userEvent.setup();
    window.history.pushState({}, '', '/finance/hot-news');

    render(
      <App
        queryRows={async () => rows}
        queryHotNewsRows={async () => hotNewsRows}
        queryMacroRows={async () => macroRows}
        queryReports={async () => []}
      />,
    );

    expect(await screen.findByRole('heading', { name: '핫 뉴스 분석 리포트' })).toBeInTheDocument();
    await user.click(screen.getByRole('tab', { name: 'AI 분석 리포트' }));

    expect(window.location.pathname).toBe('/finance/ai-reports');
    expect(screen.getByRole('heading', { name: 'AI 분석 리포트' })).toBeInTheDocument();
    expect(screen.getByText('표시할 AI 분석 리포트가 없습니다.')).toBeInTheDocument();
  });

  it('opens and closes the mobile sidebar from the dimmed area', async () => {
    const user = userEvent.setup();
    const { container } = render(
      <App
        queryRows={async () => rows}
        queryHotNewsRows={async () => hotNewsRows}
        queryMacroRows={async () => macroRows}
        queryReports={async () => []}
      />,
    );

    await user.click(screen.getByRole('button', { name: '모바일 사이드바 열기' }));
    expect(container.querySelector('.mobile-sidebar')).toHaveClass('open');
    expect(screen.queryByRole('button', { name: /닫기/ })).not.toBeInTheDocument();

    await user.click(screen.getByTestId('mobile-sidebar-dim'));
    expect(container.querySelector('.mobile-sidebar')).not.toHaveClass('open');
  });

  it('keeps consensus detail popup behavior with URL query parameters', async () => {
    window.history.pushState(
      {},
      '',
      '/finance/consensus?contentType=consensus&contentParams=%7B%22gicode%22%3A%22A005930%22%7D',
    );

    render(<App queryRows={async () => rows} queryHotNewsRows={async () => hotNewsRows} queryReports={async () => []} />);

    expect(await screen.findByRole('dialog', { name: '삼성전자 상세 분석' })).toBeInTheDocument();
    expect(within(screen.getByRole('dialog')).getByText('AI 분석 리포트가 없습니다.')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/App.test.tsx --run
```

Expected: FAIL because `App` still renders all old sections and does not use route state.

- [ ] **Step 3: Refactor `App.tsx` to route through `AppShell`**

Replace `src/App.tsx` with:

```tsx
import { useEffect, useState } from 'react';
import type { RawConsensusRow, RawSummaryReportRow } from './consensus/model';
import type { RawHotNewsReportRow } from './hot-news/model';
import type { RawMacroRegimeRow } from './macro-regime/model';
import { AiAnalysisReportsPage } from './components/AiAnalysisReportsPage';
import { AppShell } from './components/AppShell';
import { ConsensusRankingPage } from './components/ConsensusRankingPage';
import { HotNewsReportsPage } from './components/HotNewsReportsPage';
import { MacroRegimePage } from './components/MacroRegimePage';
import { getActiveRoute, navigateToPath } from './navigation';
import './styles.css';

type AppProps = {
  queryRows?: () => Promise<RawConsensusRow[]>;
  queryHotNewsRows?: () => Promise<RawHotNewsReportRow[]>;
  queryMacroRows?: () => Promise<RawMacroRegimeRow[]>;
  queryReports?: () => Promise<RawSummaryReportRow[]>;
};

export default function App({ queryRows, queryHotNewsRows, queryMacroRows, queryReports }: AppProps) {
  const [activeRoute, setActiveRoute] = useState(() => getActiveRoute(window.location.pathname));

  useEffect(() => {
    function handlePopState() {
      setActiveRoute(getActiveRoute(window.location.pathname));
    }

    window.addEventListener('popstate', handlePopState);
    handlePopState();
    return () => window.removeEventListener('popstate', handlePopState);
  }, []);

  function handleNavigate(path: string) {
    navigateToPath(path);
  }

  const page =
    activeRoute.path === '/main/macro-regime' ? (
      <MacroRegimePage queryRows={queryMacroRows} />
    ) : activeRoute.path === '/finance/hot-news' ? (
      <HotNewsReportsPage queryRows={queryHotNewsRows} />
    ) : activeRoute.path === '/finance/consensus' ? (
      <ConsensusRankingPage queryRows={queryRows} queryReports={queryReports} />
    ) : (
      <AiAnalysisReportsPage />
    );

  return (
    <AppShell activeRoute={activeRoute} onNavigate={handleNavigate}>
      {page}
    </AppShell>
  );
}
```

- [ ] **Step 4: Run route integration tests**

Run:

```bash
npm test -- src/App.test.tsx --run
```

Expected: PASS.

- [ ] **Step 5: Run related component tests**

Run:

```bash
npm test -- src/components/AppShell.test.tsx src/components/AiAnalysisReportsPage.test.tsx --run
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/App.tsx src/App.test.tsx
git commit -m "분석자료실 라우팅 통합"
```

---

### Task 5: 화이트톤 스타일 마이그레이션

**Files:**
- Modify: `src/styles.css`
- Test: `src/App.test.tsx`
- Test: `src/components/AppShell.test.tsx`

- [ ] **Step 1: Add class assertions before styling**

Append these expectations to `renders finance consensus route with finance children and underline tabs` in `src/App.test.tsx`:

```tsx
expect(document.querySelector('.workspace')).toBeInTheDocument();
expect(document.querySelector('.page-header')).toBeInTheDocument();
expect(document.querySelector('.finance-tabs')).toBeInTheDocument();
expect(document.querySelector('.dashboard')).toBeInTheDocument();
expect(document.querySelector('.hero-section')).not.toBeInTheDocument();
```

- [ ] **Step 2: Run test to verify current class gaps**

Run:

```bash
npm test -- src/App.test.tsx --run
```

Expected: PASS for classes already added by Task 3 and Task 4. If it fails, fix the component class names before changing CSS.

- [ ] **Step 3: Replace shell and base CSS tokens**

In `src/styles.css`, replace the existing root/body/shell/sidebar/top navigation blocks from `:root` through `.dashboard-section { ... }` with this block:

```css
:root {
  color: #20242a;
  background: #f4f1ea;
  font-family:
    Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
    "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
  font-synthesis: none;
  line-height: 1.5;
  text-rendering: optimizeLegibility;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  min-width: 320px;
  margin: 0;
  background: #f4f1ea;
}

body.modal-open {
  overflow: hidden;
}

a {
  color: inherit;
  text-decoration: none;
}

a:focus-visible,
button:focus-visible {
  outline: 2px solid #0f766e;
  outline-offset: 3px;
}

button {
  font: inherit;
}

.app-shell {
  display: grid;
  grid-template-columns: 286px minmax(0, 1fr);
  min-height: 100vh;
  background: #fbfbf8;
  color: #20242a;
  transition: grid-template-columns 180ms ease;
}

.app-shell.sidebar-collapsed {
  grid-template-columns: 0 minmax(0, 1fr);
}

.sidebar {
  overflow: hidden;
  border-right: 1px solid #e6e0d6;
  background: #fff;
  transition:
    transform 180ms ease,
    visibility 180ms ease;
}

.app-shell.sidebar-collapsed .sidebar {
  transform: translateX(-100%);
  visibility: hidden;
}

.brand {
  display: flex;
  width: 286px;
  align-items: center;
  gap: 10px;
  padding: 18px 18px 24px;
}

.brand-mark,
.menu-symbol,
.sub-symbol {
  display: grid;
  place-items: center;
  flex: 0 0 auto;
}

.brand-mark {
  width: 34px;
  height: 34px;
  border-radius: 8px;
  background: #0f766e;
  color: #fff;
  font-weight: 900;
}

.brand strong,
.brand small {
  display: block;
}

.brand small {
  color: #7b7165;
  font-size: 12px;
}

.nav-list {
  display: grid;
  width: 286px;
  gap: 6px;
  padding: 0 18px 18px;
}

.nav-group {
  display: grid;
  gap: 4px;
}

.nav-item,
.sub-item,
.finance-tab {
  border: 0;
  background: transparent;
  color: inherit;
  cursor: pointer;
}

.nav-item,
.sub-item {
  display: flex;
  width: 100%;
  align-items: center;
  border-radius: 8px;
  text-align: left;
}

.nav-item {
  padding: 10px;
  color: #3f474f;
  font-weight: 700;
}

.nav-item.active {
  background: #edf7f4;
  color: #0f766e;
  font-weight: 900;
}

.menu-symbol {
  width: 22px;
  height: 22px;
  margin-right: 9px;
  border-radius: 6px;
  background: #f3f0ea;
  color: #5f6770;
  font-size: 13px;
  font-weight: 900;
}

.nav-item.active .menu-symbol,
.sub-item.active .sub-symbol {
  background: #d8eee7;
  color: #0f766e;
}

.sub-list {
  display: grid;
  gap: 4px;
  margin: 2px 0 8px 16px;
}

.sub-item {
  padding: 8px 10px;
  color: #53605a;
  font-size: 13px;
}

.sub-item.active {
  background: #f5f8f6;
  color: #0f766e;
  font-weight: 900;
}

.sub-symbol {
  width: 20px;
  height: 20px;
  margin-right: 8px;
  border-radius: 6px;
  background: #f3f0ea;
  color: #6b746d;
  font-size: 12px;
  font-weight: 900;
}

.workspace {
  display: grid;
  grid-template-rows: 58px 1fr;
  min-width: 0;
}

.top-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  min-width: 0;
  padding: 0 22px;
  border-bottom: 1px solid #e6e0d6;
  background: #fff;
}

.top-nav-left,
.top-nav-actions {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}

.sidebar-toggle,
.top-nav-actions span {
  display: inline-flex;
  height: 36px;
  align-items: center;
  justify-content: center;
  border: 1px solid #ddd5ca;
  border-radius: 8px;
  background: #fff;
  color: #625a50;
  font-weight: 800;
}

.sidebar-toggle {
  width: 36px;
  padding: 0;
}

.top-nav-actions span {
  height: 34px;
  padding: 0 11px;
  font-size: 13px;
}

.mobile-toggle {
  display: none;
}

.dashboard {
  width: min(1180px, 100%);
  padding: 24px 32px 34px;
}

.page-header {
  margin-bottom: 18px;
}

.page-header span,
.section-heading span,
.summary-card span,
.card-meta,
.article-topic {
  color: #0f766e;
  font-size: 0.76rem;
  font-weight: 800;
  letter-spacing: 0;
  text-transform: none;
}

.page-header h1 {
  max-width: 780px;
  margin: 4px 0 0;
  color: #20242a;
  font-size: clamp(2rem, 4vw, 3.2rem);
  line-height: 1.08;
}

.finance-tabs {
  display: flex;
  gap: 24px;
  overflow-x: auto;
  margin-bottom: 18px;
  border-bottom: 1px solid #ded7cc;
}

.finance-tab {
  flex: 0 0 auto;
  padding: 0 0 12px;
  border-bottom: 3px solid transparent;
  color: #5f6770;
  font-size: 14px;
  white-space: nowrap;
}

.finance-tab.active,
.finance-tab[aria-selected="true"] {
  border-bottom-color: #0f766e;
  color: #0f766e;
  font-weight: 900;
}

.dashboard-section,
.analysis-section {
  border: 1px solid #e3ddd3;
  border-radius: 8px;
  background: #fff;
  box-shadow: none;
}

.dashboard-section,
.analysis-section {
  margin-top: 20px;
  padding: 28px;
}
```

- [ ] **Step 4: Add mobile drawer CSS**

Append this block near the existing responsive rules in `src/styles.css`:

```css
.mobile-sidebar-dim {
  position: fixed;
  inset: 0;
  z-index: 40;
  display: none;
  background: rgba(32, 36, 42, 0.28);
}

.mobile-sidebar-dim.visible {
  display: block;
}

.mobile-sidebar {
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  z-index: 50;
  width: 306px;
  max-width: calc(100vw - 52px);
  overflow-y: auto;
  transform: translateX(-100%);
  border-right: 1px solid #e6e0d6;
  background: #fff;
  box-shadow: 16px 0 40px rgba(0, 0, 0, 0.16);
  transition: transform 180ms ease;
}

.mobile-sidebar.open {
  transform: translateX(0);
}

.mobile-sidebar .brand,
.mobile-sidebar .nav-list {
  width: 306px;
  max-width: 100%;
}

@media (max-width: 820px) {
  .app-shell,
  .app-shell.sidebar-collapsed {
    grid-template-columns: minmax(0, 1fr);
  }

  .sidebar {
    display: none;
  }

  .workspace {
    min-height: 100vh;
  }

  .top-nav {
    padding: 0 14px;
  }

  .desktop-toggle {
    display: none;
  }

  .mobile-toggle {
    display: inline-flex;
  }

  .top-nav-actions span:last-child {
    display: none;
  }

  .dashboard {
    padding: 18px 16px 24px;
  }

  .page-header h1 {
    font-size: 1.7rem;
  }

  .finance-tabs {
    gap: 20px;
  }
}
```

- [ ] **Step 5: Run style-related tests**

Run:

```bash
npm test -- src/App.test.tsx src/components/AppShell.test.tsx --run
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/styles.css src/App.test.tsx
git commit -m "화이트톤 분석자료실 스타일 적용"
```

---

### Task 6: 기존 콘텐츠 정리와 전체 검증

**Files:**
- Modify: `src/content.ts`
- Test: all test files

- [ ] **Step 1: Remove obsolete navigation export**

In `src/content.ts`, delete the `navItems` export block:

```ts
export const navItems = [
  { label: 'Overview', href: '#overview' },
  { label: '핫뉴스 리포트', href: '#hot-news' },
  { label: '매크로 레짐', href: '#macro-regime' },
  { label: '컨센서스 괴리율 랭킹', href: '#consensus' },
  { label: 'Work', href: '#work' },
  { label: 'Writing', href: '#writing' },
  { label: 'Stack', href: '#stack' },
  { label: 'Contact', href: '#contact' },
];
```

- [ ] **Step 2: Run TypeScript build to catch unused imports**

Run:

```bash
npm run build
```

Expected: PASS. If TypeScript reports an unused import from `content.ts`, remove that import from the file named in the error and run the command again.

- [ ] **Step 3: Run all tests**

Run:

```bash
npm test -- --run
```

Expected: all test files pass.

- [ ] **Step 4: Commit final cleanup**

```bash
git add src/content.ts
git commit -m "분석자료실 전환 후 기존 내비게이션 정리"
```

---

## 자체 검토

- 설계서의 내비게이션 구조는 Task 1과 Task 3에서 구현한다.
- 기본 진입 매크로 레짐 라우팅은 Task 1과 Task 4에서 구현한다.
- 금융 하위 언더라인 탭은 Task 3과 Task 5에서 구현한다.
- 신규 AI 분석 리포트 페이지는 Task 2와 Task 4에서 구현한다.
- 모바일 드로어와 딤드 영역 닫기는 Task 3과 Task 5에서 구현한다.
- 화이트톤 스타일과 최상단 행은 Task 3과 Task 5에서 구현한다.
- 경로 문자열 미노출은 Task 3과 Task 4의 제목/라벨 구조로 충족한다.
