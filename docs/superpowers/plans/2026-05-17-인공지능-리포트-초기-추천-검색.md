# 인공지능 리포트 초기 추천 검색 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `금융 > AI 분석 리포트` 최초 진입 시 검색값과 선택 종목을 비우고, 검색어가 없을 때 `total_score` 상위 10개 리포트 항목을 추천 칩으로 보여준다.

**Architecture:** 데이터 정규화와 Supabase 조회는 그대로 둔다. `AiAnalysisReportsPage`에서 검색 결과 표시 목록과 활성 선택 상태만 파생해 자동 선택을 제거한다. 테스트는 컴포넌트 동작 중심으로 실패 사례를 먼저 추가한 뒤 최소 구현으로 통과시킨다.

**Tech Stack:** React 19, TypeScript, Vite, Vitest, Testing Library

---

## 파일 구조

- Modify: `src/components/AiAnalysisReportsPage.test.tsx`
  - 초기 미선택, `total_score` 상위 10개 추천, 빈 검색 제출, 선택 후 상세 렌더링을 검증한다.
- Modify: `src/components/AiAnalysisReportsPage.tsx`
  - 검색어가 없을 때 사용할 전체 리포트 항목 기준 추천 목록 파생값을 만들고, 자동 선택 fallback을 제거한다.
  - 선택 리포트가 없을 때 검색 패널과 미선택 안내 패널을 렌더링한다.
- Modify: `src/styles.css`
  - 필요한 경우 기존 `state-panel`, `ai-report-layout`, `ai-report-score-grid` 계열을 재사용하는 최소 스타일만 추가한다.
- Modify: `src/App.test.tsx`
  - 금융 탭으로 AI 리포트 지면에 진입하는 통합 테스트가 새 초기 미선택 정책을 기대하도록 갱신한다.

## Task 1: 실패 테스트 추가

**Files:**
- Modify: `src/components/AiAnalysisReportsPage.test.tsx`

- [ ] **Step 1: 테스트 데이터 헬퍼 추가**

`lgChemRow` 아래에 다음 헬퍼를 추가한다.

```tsx
function createRecommendationRow(index: number, totalScore: number, updatedDay: number) {
  return createSamsungRow({
    id: `recommendation-${index}`,
    stock_code: String(100000 + index),
    stock_name: `추천종목${index}`,
    total_score: totalScore,
    updated_at: `2026-05-${String(updatedDay).padStart(2, '0')}T09:00:00+00:00`,
  });
}
```

- [ ] **Step 2: 최초 진입 테스트 추가**

`renders an empty state when no reports are available` 테스트 다음에 아래 테스트를 추가한다.

```tsx
it('starts with an empty search input, no selected stock, and top total-score recommendations', async () => {
  const rows = [
    createRecommendationRow(1, 41, 17),
    createRecommendationRow(2, 92, 10),
    createRecommendationRow(3, 88, 11),
    createRecommendationRow(4, 79, 12),
    createRecommendationRow(5, 78, 13),
    createRecommendationRow(6, 77, 14),
    createRecommendationRow(7, 76, 15),
    createRecommendationRow(8, 75, 16),
    createRecommendationRow(9, 74, 17),
    createRecommendationRow(10, 73, 18),
    createRecommendationRow(11, 72, 19),
    createRecommendationRow(12, 71, 20),
  ];

  render(<AiAnalysisReportsPage queryRows={async () => rows} />);

  const search = await screen.findByRole('search', { name: 'AI 분석 리포트 검색' });
  const searchInput = within(search).getByRole('searchbox', { name: '종목명 또는 종목코드 검색' });

  expect(searchInput).toHaveValue('');
  expect(within(search).getAllByRole('button').map((button) => button.textContent)).toEqual([
    '검색',
    '추천종목2 100002 · 1건',
    '추천종목3 100003 · 1건',
    '추천종목4 100004 · 1건',
    '추천종목5 100005 · 1건',
    '추천종목6 100006 · 1건',
    '추천종목7 100007 · 1건',
    '추천종목8 100008 · 1건',
    '추천종목9 100009 · 1건',
    '추천종목10 100010 · 1건',
    '추천종목11 100011 · 1건',
  ]);
  expect(within(search).queryByRole('button', { name: /추천종목1 100001/ })).not.toBeInTheDocument();
  expect(within(search).queryByRole('button', { name: /추천종목12 100012/ })).not.toBeInTheDocument();
  expect(screen.getByText('선택된 종목이 없습니다.')).toBeInTheDocument();
  expect(screen.getByText('리포트를 선택해 분석 내용을 확인하세요.')).toBeInTheDocument();
  expect(screen.queryByRole('region', { name: /추천종목2 리포트 이력/ })).not.toBeInTheDocument();
  expect(screen.queryByRole('region', { name: '선택 리포트 분석 결과' })).not.toBeInTheDocument();
  expect(screen.queryByRole('group', { name: '점수 선택' })).not.toBeInTheDocument();
});
```

- [ ] **Step 3: 빈 검색 제출 테스트 추가**

위 테스트 다음에 아래 테스트를 추가한다.

```tsx
it('does not auto-select the first recommendation when submitting an empty search', async () => {
  const user = userEvent.setup();

  render(<AiAnalysisReportsPage queryRows={async () => [latestSamsungRow, lgChemRow]} />);

  const search = await screen.findByRole('search', { name: 'AI 분석 리포트 검색' });
  await user.click(within(search).getByRole('button', { name: '검색' }));

  expect(within(search).getByRole('searchbox', { name: '종목명 또는 종목코드 검색' })).toHaveValue('');
  expect(screen.getByText('선택된 종목이 없습니다.')).toBeInTheDocument();
  expect(screen.queryByRole('region', { name: '삼성전자 리포트 이력' })).not.toBeInTheDocument();
  expect(screen.queryByRole('region', { name: '선택 리포트 분석 결과' })).not.toBeInTheDocument();
});
```

- [ ] **Step 4: 기존 상세 테스트의 초기 선택 기대값 수정**

`renders search entry...` 테스트의 초반부를 다음 흐름으로 바꾼다.

```tsx
const search = await screen.findByRole('search', { name: 'AI 분석 리포트 검색' });
const searchInput = within(search).getByRole('searchbox', { name: '종목명 또는 종목코드 검색' });
expect(searchInput).toHaveValue('');
expect(within(search).getByRole('button', { name: /삼성전자 005930/ })).not.toHaveClass('active');
expect(within(search).getByRole('button', { name: /LG화학 051910/ })).toBeInTheDocument();
expect(screen.queryByRole('region', { name: '종목 대표 목록' })).not.toBeInTheDocument();
expect(screen.queryByRole('region', { name: '삼성전자 리포트 이력' })).not.toBeInTheDocument();

await user.click(within(search).getByRole('button', { name: /삼성전자 005930/ }));

expect(searchInput).toHaveValue('삼성전자');
expect(within(search).getByRole('button', { name: /삼성전자 005930/ })).toHaveClass('active');
expect(within(search).queryByRole('button', { name: /LG화학 051910/ })).not.toBeInTheDocument();
```

- [ ] **Step 5: 사라진 선택 종목 테스트 수정**

마지막 테스트 이름을 아래로 바꾼다.

```tsx
it('returns to an unselected state when a selected stock disappears after reload', async () => {
```

마지막 기대값을 아래로 바꾼다.

```tsx
expect(await screen.findByText('선택된 종목이 없습니다.')).toBeInTheDocument();
expect(screen.queryByRole('region', { name: 'LG화학 리포트 이력' })).not.toBeInTheDocument();
expect(screen.queryByRole('region', { name: '삼성전자 리포트 이력' })).not.toBeInTheDocument();
```

- [ ] **Step 6: 실패 확인**

Run:

```bash
rtk npm test -- src/components/AiAnalysisReportsPage.test.tsx
```

Expected: FAIL. 실패 이유는 검색 입력이 `삼성전자`로 채워지고 첫 대표 리포트가 자동 선택되는 기존 동작이어야 한다.

## Task 2: 페이지 상태와 미선택 UI 구현

**Files:**
- Modify: `src/components/AiAnalysisReportsPage.tsx`
- Modify: `src/styles.css`

- [ ] **Step 1: 검색 컴포넌트에 표시 목록 계산 추가**

`AiReportSearch` 내부의 `filteredReports` 계산을 다음으로 교체한다.

```tsx
  const normalizedQuery = query.trim().toLowerCase();
  const hasSearchQuery = normalizedQuery.length > 0;
  const indexedReports = reports.map((report, index) => ({ report, index }));
  const filteredReports = hasSearchQuery
    ? indexedReports
        .filter(
          ({ report }) =>
            report.stockName.toLowerCase().includes(normalizedQuery) ||
            report.stockCode.toLowerCase().includes(normalizedQuery),
        )
        .map(({ report }) => report)
    : [...indexedReports]
        .sort((left, right) => {
          const leftScore = left.report.totalScore;
          const rightScore = right.report.totalScore;

          if (leftScore !== null && rightScore === null) {
            return -1;
          }

          if (leftScore === null && rightScore !== null) {
            return 1;
          }

          if (leftScore !== null && rightScore !== null && rightScore !== leftScore) {
            return rightScore - leftScore;
          }

          if (right.report.sortTimestamp !== left.report.sortTimestamp) {
            return right.report.sortTimestamp - left.report.sortTimestamp;
          }

          return left.index - right.index;
        })
        .slice(0, 10)
        .map(({ report }) => report);
```

- [ ] **Step 2: 빈 검색 제출 자동 선택 방지**

`handleSubmit`을 다음으로 교체한다.

```tsx
  function handleSubmit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();

    if (!hasSearchQuery) {
      return;
    }

    const firstReport = filteredReports[0];
    if (firstReport) {
      onSelect(firstReport.stockCode);
      onQueryChange(firstReport.stockName);
    }
  }
```

- [ ] **Step 3: 자동 활성 종목 fallback 제거**

`AiAnalysisReportsPage`의 활성 종목 파생값을 다음으로 바꾼다.

```tsx
  const selectedRepresentativeExists = representatives.some((report) => report.stockCode === selectedStockCode);
  const activeStockCode = selectedRepresentativeExists ? selectedStockCode : null;
  const displayedSearchQuery = searchQuery ?? '';
  const history = useMemo(() => (activeStockCode ? selectReportHistory(reports, activeStockCode) : []), [activeStockCode, reports]);
  const selectedReport = activeStockCode ? history.find((report) => report.id === selectedReportId) ?? history[0] ?? null : null;
```

기존 `activeRepresentative` 변수는 삭제한다.

- [ ] **Step 4: 검색 패널은 성공 상태에서 항상 렌더링**

성공 상태 렌더링을 아래 구조로 바꾼다. 기존 상세 JSX는 `selectedReport ? (...) : (...)`의 true 분기 안에 그대로 둔다.

```tsx
      {state.status === 'success' && representatives.length > 0 ? (
        <>
          <AiReportSearch
            activeStockCode={activeStockCode}
            query={displayedSearchQuery}
            reports={representativeSearchReports}
            onQueryChange={setSearchQuery}
            onSelect={handleSelectStock}
          />

          {selectedReport ? (
            <div className="ai-report-layout">
              {/* 기존 상세 레이아웃 유지 */}
            </div>
          ) : (
            <div className="ai-report-layout">
              <div className="ai-report-sidebar">
                <section className="ai-report-panel" aria-label="리포트 이력 미선택 상태">
                  <div className="ai-report-panel-title">
                    <span>리포트 이력</span>
                    <strong>미선택</strong>
                  </div>
                  <p className="ai-report-muted">선택된 종목이 없습니다.</p>
                </section>
              </div>
              <div className="ai-report-main">
                <section className="ai-report-empty-detail state-panel" aria-label="상세 미선택 상태">
                  <strong>리포트를 선택해 분석 내용을 확인하세요.</strong>
                  <p>상단의 추천 종목이나 검색 결과를 선택하면 분석 본문과 점수 카드가 표시됩니다.</p>
                  <div className="ai-report-score-grid ai-report-score-grid-placeholder" aria-hidden="true">
                    {scoreCards.map((score) => (
                      <div className="ai-report-score-card" key={score.key}>
                        <span>{score.label}</span>
                        <strong>-</strong>
                      </div>
                    ))}
                  </div>
                </section>
              </div>
            </div>
          )}
        </>
      ) : null}
```

- [ ] **Step 5: 미선택 점수 카드 최소 스타일 추가**

`src/styles.css`의 AI 리포트 스타일 근처에 아래 CSS를 추가한다.

```css
.ai-report-empty-detail {
  display: grid;
  gap: 14px;
}

.ai-report-score-grid-placeholder {
  margin-top: 4px;
  opacity: 0.55;
}
```

- [ ] **Step 6: 대상 테스트 통과 확인**

Run:

```bash
rtk npm test -- src/components/AiAnalysisReportsPage.test.tsx
```

Expected: PASS.

## Task 3: 전체 검증과 커밋

**Files:**
- Verify only

- [ ] **Step 1: 전체 테스트 실행**

Run:

```bash
rtk npm test
```

Expected: `28 passed`, 모든 테스트 통과.

- [ ] **Step 2: 빌드 실행**

Run:

```bash
rtk npm run build
```

Expected: TypeScript와 Vite 빌드가 성공하고 종료 코드 0.

- [ ] **Step 3: 변경사항 확인**

Run:

```bash
rtk git diff -- src/components/AiAnalysisReportsPage.tsx src/components/AiAnalysisReportsPage.test.tsx src/styles.css
```

Expected: 테스트, 페이지 상태, 최소 스타일만 변경되어야 한다.

- [ ] **Step 4: 구현 커밋**

Run:

```bash
rtk git add src/components/AiAnalysisReportsPage.tsx src/components/AiAnalysisReportsPage.test.tsx src/styles.css
rtk git commit -m "인공지능 리포트 초기 추천 검색 구현"
```

Expected: 구현 커밋 1개 생성.

## Task 4: 라우팅 통합 테스트 갱신

**Files:**
- Modify: `src/App.test.tsx`

- [ ] **Step 1: 실패 원인 확인**

Run:

```bash
rtk npm test -- src/App.test.tsx
```

Expected: `navigates between finance pages from underline tabs`가 실패한다. 실패 지점은 `삼성전자` 상세 제목을 찾는 기대값이다. 새 정책에서는 AI 리포트 탭 진입 직후 상세가 자동 선택되지 않는다.

- [ ] **Step 2: 통합 테스트 기대값 수정**

`src/App.test.tsx`의 `navigates between finance pages from underline tabs` 테스트에서 아래 기대값을 제거한다.

```tsx
expect(await screen.findByRole('heading', { name: '삼성전자' })).toBeInTheDocument();
expect(screen.getAllByText('totalScore 69').length).toBeGreaterThan(0);
```

대신 아래 기대값을 추가한다.

```tsx
const aiSearch = await screen.findByRole('search', { name: 'AI 분석 리포트 검색' });
expect(within(aiSearch).getByRole('searchbox', { name: '종목명 또는 종목코드 검색' })).toHaveValue('');
expect(within(aiSearch).getByRole('button', { name: /삼성전자 005930/ })).toBeInTheDocument();
expect(screen.getByText('선택된 종목이 없습니다.')).toBeInTheDocument();
expect(screen.getByText('리포트를 선택해 분석 내용을 확인하세요.')).toBeInTheDocument();
expect(screen.queryByRole('region', { name: '삼성전자 리포트 이력' })).not.toBeInTheDocument();
expect(screen.queryByRole('region', { name: '선택 리포트 분석 결과' })).not.toBeInTheDocument();
```

- [ ] **Step 3: 통합 테스트 통과 확인**

Run:

```bash
rtk npm test -- src/App.test.tsx
```

Expected: PASS.

- [ ] **Step 4: 전체 테스트와 빌드 재실행**

Run:

```bash
rtk npm test
rtk npm run build
```

Expected: 전체 테스트와 빌드가 모두 통과한다.

- [ ] **Step 5: 통합 테스트 커밋**

Run:

```bash
rtk git add src/App.test.tsx docs/superpowers/plans/2026-05-17-인공지능-리포트-초기-추천-검색.md
rtk git commit -m "인공지능 리포트 라우팅 테스트 갱신"
```

Expected: 통합 테스트와 계획 보강 커밋 1개 생성.

---

## 자체 점검

- 설계서의 최초 검색 빈 값, 자동 선택 제거, `total_score` 상위 10개 노출 요구를 Task 1과 Task 2가 모두 다룬다.
- Supabase 조회, 모델 정규화, 상세 본문 구조는 범위 제외로 유지한다.
- 테스트는 실패 확인 후 구현하는 순서다.
- 작업 파일은 컴포넌트, 컴포넌트 테스트, 최소 CSS로 제한한다.
